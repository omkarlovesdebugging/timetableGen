  # subject_name = Label(subject_frame, text=list[i][j], font=("Arial", 8, "bold"), bg="#C1BBEB", fg="#4a148c")
                    # subject_name.pack(side=TOP, anchor=W, padx=0, pady=0)

                    # # Create a label for the teacher name
                    # teacher_name = Label(subject_frame, text=list[i][j], font=("Arial", 8), bg="#C1BBEB", fg="#4a148c")
                    # teacher_name.pack(side=TOP, anchor=W, padx=0, pady=0)

                    # # Create a label for the room number
                    # room_number = Label(subject_frame, text=list[i][j], font=("Arial", 8), bg="#C1BBEB", fg="#4a148c")
                    # room_number.pack(side=TOP, anchor=W,